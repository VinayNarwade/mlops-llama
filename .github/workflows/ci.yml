name: MLOps CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests
        run: pytest tests/ -v --tb=short

  build-and-push:
    name: Build and push Docker image
    runs-on: ubuntu-latest
    needs: test                    # only runs if tests pass

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/llama-api:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/llama-api:${{ github.sha }}

      - name: Smoke test the image
        run: |
          docker run -d --name test-api -p 8000:8000 \
            ${{ secrets.DOCKERHUB_USERNAME }}/llama-api:latest
          sleep 5
          curl --fail http://localhost:8000/health
          docker stop test-api

      - name: Print image digest
        run: echo "Image pushed successfully"
```

The pipeline now does this on every push:
```
git push
    ↓
[test job]         ← pytest runs
    ↓ only if green
[build-and-push]   ← builds Docker image
                   ← pushes to Docker Hub with two tags:
                      • latest        (always points to newest)
                      • <git sha>     (immutable, traceable forever)
                   ← smoke tests /health endpoint